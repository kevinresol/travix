name: Build

on:
  push:
    branches:
    - '**'
    tags-ignore:
    - '**'
    paths-ignore:
    - '**/*.md'
    - provision.sh
    - Vagrantfile
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  # https://docs.brew.sh/Manpage#environment
  HOMEBREW_NO_ANALYTICS: 1
  HOMEBREW_NO_AUTO_UPDATE: 1
  HOMEBREW_NO_INSTALL_CLEANUP: 1
  HOMEBREW_NO_INSTALLED_DEPENDENTS_CHECK: 1
  HOMEBREW_NO_REQUIRE_TAP_TRUST: 1

jobs:
  build:
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest
        haxe:
          - 4.2.5
          - stable
          - 5.0.0-preview.1
          - nightly

    steps:
    - name: Git Checkout
      uses: actions/checkout@v7

    - name: Setup Haxe ${{ matrix.haxe }}
      if: runner.os != 'Windows'
      uses: krdlab/setup-haxe@v2
      with:
        haxe-version: ${{ matrix.haxe }}
        cache-dependency-path: haxelib.json

    - name: Update brew formulas
      if: runner.os == 'macOS'
      run: |
        echo "::group::brew update" && brew update && echo "::endgroup::"
        echo "::group::brew config" && brew config && echo "::endgroup::"
        echo "::group::brew list" && brew list --version && echo "::endgroup::"

        # workaround to prevent "Error: The `brew link` step did not complete successfully" during "brew install mono"
        set +e
        rm -f \
          /usr/local/share/man/man1/* \
          /usr/local/share/man/man5/* \
          /usr/local/bin/idle3 \
          /usr/local/bin/2to3 \
          /usr/local/bin/pydoc3 \
          /usr/local/bin/python3 \
          /usr/local/bin/python3-config \
          /opt/homebrew/share/man/man1/* \
          /opt/homebrew/share/man/man5/* \
          /opt/homebrew/bin/idle3 \
          /opt/homebrew/bin/2to3 \
          /opt/homebrew/bin/pydoc3 \
          /opt/homebrew/bin/python3 \
          /opt/homebrew/bin/python3-config
        set -e

    - name: Set up Python 3
      uses: actions/setup-python@v7
      with:
        python-version: "3.13"

    - name: Configure Python 3 on Windows
      if: runner.os == 'Windows'
      shell: cmd
      # workaround for https://github.com/actions/setup-python/issues/123
      run:
        if not exist "%pythonLocation%\python.exe" (
          mklink "%pythonLocation%\python3.exe" "%pythonLocation%\python.exe"
        )

    - name: Setup Node.js
      uses: actions/setup-node@v7
      with:
        node-version: 22

    - name: Setup lix
      shell: bash
      run: npm i -g lix

    - name: Setup Haxe via lix (Windows)
      if: runner.os == 'Windows'
      shell: bash
      run: lix install haxe ${{ matrix.haxe }}

    - name: Install haxe libs
      shell: bash
      run: lix download

    - name: Build Travix
      shell: bash
      run: haxe build-neko.hxml -lib hx4compat

    - name: Set test HAXE_LIBS
      shell: bash
      run: |
        if [ "${{ matrix.haxe }}" != "4.2.5" ]; then
          echo "HAXE_LIBS=-lib hx4compat" >> "$GITHUB_ENV"
        fi

    - name: Prepare isolated test workspace
      shell: bash
      env:
        HAXE_VERSION: ${{ matrix.haxe }}
      run: node scripts/prepare-test-workspace.mjs

    - name: Test [eval]
      working-directory: ${{ env.TEST_WORKSPACE }}
      run: lix run travix interp -D eval-stack ${HAXE_LIBS:-}

    - name: Test Travix with [hl]
      if: runner.os == 'Windows'
      working-directory: ${{ env.TEST_WORKSPACE }}
      run: lix run travix hl ${HAXE_LIBS:-}

    - name: Test Travix with [interp]
      working-directory: ${{ env.TEST_WORKSPACE }}
      run: lix run travix interp ${HAXE_LIBS:-}

    - name: Test Travix with [lua]
      continue-on-error: ${{ runner.os != 'Linux' }}
      working-directory: ${{ env.TEST_WORKSPACE }}
      run: lix run travix lua ${HAXE_LIBS:-}

    - name: Test Travix with [neko]
      working-directory: ${{ env.TEST_WORKSPACE }}
      run: lix run travix neko ${HAXE_LIBS:-}

    - name: Test Travix with [node]
      working-directory: ${{ env.TEST_WORKSPACE }}
      run: lix run travix node ${HAXE_LIBS:-}

    - name: Test Travix with [php]
      working-directory: ${{ env.TEST_WORKSPACE }}
      run: lix run travix php ${HAXE_LIBS:-}

    - name: Test Travix with [python]
      working-directory: ${{ env.TEST_WORKSPACE }}
      run: lix run travix python ${HAXE_LIBS:-}

    - name: Test Travix with [jvm]
      working-directory: ${{ env.TEST_WORKSPACE }}
      run: lix run travix jvm ${HAXE_LIBS:-}

    - name: Test Travix with [js]
      working-directory: ${{ env.TEST_WORKSPACE }}
      run: lix run travix js ${HAXE_LIBS:-}
